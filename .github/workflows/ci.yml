name: CI Workflow

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Run Command
        run: |
          # Now, you can run your desired command(s)
          echo "Hello, world!"
          # Add more commands as needed
          npm run prod
